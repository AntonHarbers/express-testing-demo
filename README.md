# Express Testing Demo - The Odin Project

A demo of express route testing using the supertest library for the odin project.

In order to run the tests install the dependencies and run

```cmd
$ npm run test
```
